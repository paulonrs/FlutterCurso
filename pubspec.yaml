name: flutter_curso
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: '3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  meta:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  test:
  mockito: 5.4.2
  faker:

flutter:
  uses-material-design: true

